# แอปศูนย์รวมระบบ (PWA)

สาขาวิชาสาธารณสุขศาสตร์ มหาวิทยาลัยราชภัฏลำปาง

แอปจริงที่ติดตั้งลงมือถือได้ มีไอคอนบนหน้าจอ เปิดเต็มจอไม่มีแถบเบราว์เซอร์ และใช้ได้แม้ไม่มีอินเทอร์เน็ต

---

## ต่างจากหน้ารวมที่ทำบน Apps Script อย่างไร

| | บน Apps Script | **แอปนี้ (PWA)** |
|---|---|---|
| ไอคอนบนหน้าจอโฮม | ได้ (เป็นลิงก์) | ได้ (เป็นแอปจริง) |
| เปิดเต็มจอไม่มีแถบเบราว์เซอร์ | ไม่ได้ | **ได้** |
| ใช้ตอนไม่มีเน็ต | ไม่ได้ | **ได้** |
| Android ขึ้นปุ่ม "ติดตั้งแอป" ให้เอง | ไม่ได้ | **ได้** |
| ติดปัญหาสิทธิ์ของบัญชีมหาวิทยาลัย | ติด | **ไม่ติด** |
| ใช้ GPS / กล้อง ได้ในอนาคต | ไม่ได้ | **ได้** |
| ต้องมีโฮสต์ | ไม่ต้อง | ต้องมี (ฟรี) |

ข้อแลกเปลี่ยนเดียวคือต้องหาที่วางไฟล์ ซึ่งมีบริการฟรีที่ใช้เวลาตั้งไม่ถึง 5 นาที

---

## ไฟล์ในโฟลเดอร์นี้

| ไฟล์ | หน้าที่ | ต้องแก้ไหม |
|---|---|---|
| `index.html` | ตัวแอป หน้าไทล์ทั้งหมด | ไม่ต้อง |
| `systems.json` | **รายการระบบและลิงก์** | แก้ผ่านหน้าแอป |
| `manifest.json` | บอกมือถือว่านี่คือแอป ชื่ออะไร ไอคอนอะไร | ไม่ต้อง |
| `sw.js` | ทำให้ใช้งานได้ตอนไม่มีเน็ต | ไม่ต้อง |
| `icons/` | ไอคอนแอปจากตรามหาวิทยาลัย | ไม่ต้อง |

---

## ติดตั้ง — วิธีที่ 1 : Netlify (ง่ายที่สุด แนะนำ)

1. ไปที่ [app.netlify.com/drop](https://app.netlify.com/drop)
2. **ลากทั้งโฟลเดอร์** `แอปศูนย์รวมระบบ` ไปวางในกรอบบนหน้าเว็บ
3. รอสักครู่ จะได้ลิงก์แบบ `https://ชื่อสุ่ม.netlify.app`
4. สมัครบัญชีฟรี (ใช้อีเมลมหาวิทยาลัยได้) เพื่อให้ลิงก์อยู่ถาวรและตั้งชื่อเองได้
5. ตั้งชื่อใหม่ที่ Site configuration → Change site name เช่น `ph-lpru`
   จะได้ `https://ph-lpru.netlify.app`

> ⚠️ **ข้อควรระวังที่สำคัญที่สุดของ Netlify Drop**
>
> ทุกครั้งที่อัปเดต ต้องลาก **ทั้งโฟลเดอร์** ไปวางใหม่เสมอ
> **ห้ามลากไฟล์เดียวไปวาง** เพราะ Netlify จะถือว่าเว็บทั้งเว็บมีแค่ไฟล์นั้นไฟล์เดียว
> ไฟล์ที่เหลือจะหายหมดและแอปจะเปิดไม่ได้
>
> วิธีที่ปลอดภัย : เอาไฟล์ `systems.json` ที่ดาวน์โหลดมาใหม่
> ไปวางทับในโฟลเดอร์บนเครื่องก่อน แล้วค่อยลากทั้งโฟลเดอร์ขึ้นไป

## ติดตั้ง — วิธีที่ 2 : GitHub Pages

1. สมัคร [github.com](https://github.com) (ใช้อีเมลมหาวิทยาลัยได้)
2. สร้าง repository ใหม่ ตั้งชื่อเช่น `ph-lpru-hub` เลือก **Public**
3. กด **uploading an existing file** แล้วลากไฟล์ทั้งหมดขึ้นไป (รวมโฟลเดอร์ `icons`)
4. ไปที่ **Settings → Pages → Source: Deploy from a branch → main → /(root) → Save**
5. รอ 1–2 นาที จะได้ `https://ชื่อผู้ใช้.github.io/ph-lpru-hub/`

---

## ใส่ลิงก์ของแต่ละระบบ

1. เปิดแอปที่ได้จากขั้นตอนข้างบน
2. แตะรูปเฟือง **⚙** มุมขวาบน
3. วางลิงก์ของแต่ละระบบลงในช่อง (ระบบที่เว้นว่างจะไม่แสดงบนหน้าแรก)
4. กด **ดาวน์โหลดไฟล์** จะได้ไฟล์ `systems.json` ใหม่ (อยู่ในโฟลเดอร์ดาวน์โหลด)
5. นำไฟล์นั้นไป**วางทับ** `systems.json` เดิมในโฟลเดอร์ `แอปศูนย์รวมระบบ` บนเครื่อง
6. ลาก **ทั้งโฟลเดอร์** ไปวางที่ [app.netlify.com/drop](https://app.netlify.com/drop) อีกครั้ง
   (ถ้าสมัครบัญชีแล้ว ให้เข้าเว็บไซต์ของตัวเอง → แท็บ **Deploys** → ลากวางตรงนั้น)
7. รีเฟรชหน้าแอป จะเห็นรายการใหม่ทุกเครื่อง

> **ทำไมต้องดาวน์โหลดแล้วอัปโหลด** — แอปนี้ไม่มีเซิร์ฟเวอร์หลังบ้าน จึงเขียนไฟล์เองไม่ได้
> วิธีนี้แลกความยุ่งยากตอนแก้ กับการที่แอปทำงานเร็ว ฟรี และไม่มีอะไรพังได้
>
> ถ้ายังไม่ได้อัปโหลด รายการใหม่จะแสดงเฉพาะบนเครื่องที่แก้เท่านั้น

---

## ติดตั้งลงมือถือ

**iPhone / iPad** — ต้องเปิดด้วย **Safari** เท่านั้น
แตะปุ่มแชร์ (สี่เหลี่ยมมีลูกศรขึ้น) → เลื่อนหา **เพิ่มไปยังหน้าจอโฮม** → **เพิ่ม**

**Android** — เปิดด้วย **Chrome**
จะมีปุ่ม **ติดตั้งเป็นแอปบนเครื่องนี้** เด้งขึ้นมาที่ด้านล่างจอ แตะแล้วกด **ติดตั้ง**
หรือแตะจุดสามจุดมุมขวาบน → **ติดตั้งแอป**

เปิดจากไอคอนแล้วจะเต็มจอ ไม่มีแถบที่อยู่เว็บ เหมือนแอปทั่วไป

---

## แก้ไขตัวแอปในอนาคต

ถ้าแก้ `index.html` หรือไฟล์อื่นที่ไม่ใช่ `systems.json` ต้อง**เปลี่ยนเลขรุ่นแคช**ด้วย
มิฉะนั้นเครื่องที่ติดตั้งไปแล้วจะยังใช้ของเก่าค้างอยู่

เปิด `sw.js` บรรทัดบน ๆ แล้วเปลี่ยนเลข

```javascript
var รุ่นแคช = 'ศูนย์รวมระบบ-v1';   →   'ศูนย์รวมระบบ-v2'
```

แล้วอัปโหลดใหม่ทั้งชุด

---

## ข้อจำกัดที่ควรรู้

- **ไม่มีระบบล็อกอิน** ใครมีลิงก์ก็เปิดหน้ารวมได้ แต่ระบบปลายทางแต่ละตัวยังมีการล็อกอินของตัวเองตามเดิม
- **ปุ่มเฟืองไม่มีรหัสผ่าน** เพราะเป็นเว็บสถิต ป้องกันจริงไม่ได้ — แต่การแก้จะมีผลเฉพาะเครื่องนั้น
  จนกว่าจะอัปโหลดไฟล์ทับ ผู้ที่ไม่มีสิทธิ์เข้าโฮสต์จึงแก้ของส่วนกลางไม่ได้
- **ยังไม่มีการแจ้งเตือน push** ต้องมีเซิร์ฟเวอร์เพิ่ม ถ้าอยากได้บอกได้
- ต้องเปิดผ่าน **https** จึงจะติดตั้งเป็นแอปได้ (Netlify และ GitHub Pages ให้มาอยู่แล้ว)

---

## ถ้ามหาวิทยาลัยให้ subdomain มา

ยกไฟล์ทั้งชุดไปวางบนพื้นที่นั้นได้เลย ไม่ต้องแก้อะไรในโค้ด
แล้วจะได้ที่อยู่แบบ `https://ph.lpru.ac.th` ซึ่งน่าเชื่อถือกว่าและอยู่ในโดเมนของมหาวิทยาลัย

หลังจากนั้นจะพัฒนาต่อได้อีก เช่น ย้ายหน้าสแกนเช็คชื่อมาไว้ที่นี่
เพื่อให้**ใช้ GPS ตรวจว่านักศึกษาอยู่ในห้องเรียนจริงได้** ซึ่งทำบน Apps Script ไม่ได้
